import { getCategories } from './categories';
import { getPosts, createPost, editPost, upvotePost, downvotePost } from './posts';
import { getComments, createComment, upvoteComment, downvoteComment } from './comments';

export default {
  getCategories,
  getPosts,
  createPost,
  editPost,
  upvotePost,
  downvotePost,
  getComments,
  createComment,
  upvoteComment,
  downvoteComment
}